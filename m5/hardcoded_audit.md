# 写死残留审查清单（2026-08-21——用户："还有写死的模板吗——工程要靠着理论——
这个 AI 框架之前完全没有过——惯性思维虽在正常 AI 框架内对但本框架不同——随时记住理论"）

## 审查结果（写死项——对照理论——违反 C15-01/C2-02）

| 写死项 | 位置 | 理论违反 | 自发方案 |
|---|---|---|---|
| **FUNC_WORDS**（是/很/有/因为…功能词表） | stage47/49/61 | **C15-01**（语法=时序模板——统计自发生成——非先天规则库）——直接违反 | 自发枢纽（stage78 已做 hubs——统计涌现——推广到所有用功能词处） |
| **REL_IDX 四层**（isa/attr/act/cause 预设） | stage40-43/57/61 | C2-02（关系类型 = 训练产物） | 枢纽字聚类（stage49 方向——经字槽位分布聚类——未完成） |
| **ACT_VERBS**（动词表——吃/看/写…） | stage40/41/43 | C15-02（词类=分布聚类涌现） | 位置分布（stage52——句中位——自发） |
| **NEG_WORDS**（不是/没有/不——否定词表） | stage57 | R45（否定=反例标记——应统计涌现） | 反例句的统计模式（"不是"高频反例结构——从数据） |
| **STOP_CHARS**（。！？吗呢吧啊——句尾字表） | stage46 | A0（句界 = 预测骤降——误差词汇） | Goriely 预测骤降（stage54——自发句界） |

## 惯性思维诊断（为什么写死——正常 AI 框架的 NLP 词表/规则思维）

```
我的惯性（正常 AI 框架内对）：功能词=词表 / 关系类型=分类 / 动词否定词=词典
框架不同（用户提醒）：C15-01/C2-02——一切训练产物——"规则"=关系统计的
  压缩描述（C122-01）——功能词/关系/词类/否定/句界——全部从数据生长
```

## 修正原则（固化习惯）

```
每个工程决策前自问：这个"表/类/规则"是写死的还是涌现的？
——写死（词表/分类/阈值规则）→ 找理论对应（C/R）→ 改自发机制
——工程靠着理论（C/R 猜想锚定）——不是 NLP 经验
```

## 项目状态（compact 锚点）

**文字湖 41 阶段**（全部提交——本地 68 提交——云端 stage75）：
```
学习链：单字→课程（K图谱）→密集→稀疏→误差→价值→睡眠→关键期→记忆（26s/5天——46×性能修复）
理解链：转导→湖→层级→关系→因果→否定→问答
生成链：组合→时序→预测切分→模板（句来填字——C20-01 自发枢纽）→trigram→边界
里程碑：苹果→苹果很甜（完整句）/小猫吃→鱼（多义区分）/规模 3574 句 108s
```

## 待办（按清单修正写死项——优先级）

```
① 功能词表→自发枢纽 —— ✅ stage79 完成（2026-08-20）
    词块先行（相邻共现率>0.5——"因为"94/94=1.0 涌现——覆盖块内单字）
    + 单字枢纽（频率×上下文熵×位置集中度——"是/很"涌现）
    —— 对照 5/7 命中（"有"被"没有"块覆盖/"属于"低频——统计诚实）
    模板 [因为 X 所以 X]×60 涌现（替换 FUNC_WORDS 查找）
② 关系四层→枢纽河道 —— ✅ stage79 完成（K={枢纽: 矩阵}——无 RELS/触发词）
    K[很][苹果]→甜脆 / K[因为][带伞]→雨 / K[因为][穿棉衣]→冷
    问句自发解析："为什么带伞？"→"为"→"因为"块反查→雨
    （"为什么/是什么"问标记与语料零共现——零河道原理——正确行为）
③ 动词表→位置分布 —— ✅ stage80 完成（2026-08-21）
    动词性 = 中位偏置×位置集中×上下文熵（纯位置特征——C15-02——去频率）
    ——"开/吃/种"涌现 top25；位置对比：吃(mean 0.46,std 0.19) vs 苹(0.5 分)
    ——动词位 vs 主语位分布区分
    动作河道：K[吃][猫]→鱼/K[吃][老鼠]→猫（"吃"经基础枢纽机制自发——
      freq 42+位置集中——无 ACT_VERBS）
    动作问答："猫吃什么？"→'吃'河道→鱼（与"为什么"对称）
    已知局限："苹果可以吃吗？"→'可'（"吗"语料 0 次——确认问未建模——
      零河道边缘）；低频动作字（喝/看/写<30 次）不进单字枢纽——语料覆盖
    stage40-43 的 ACT_VERBS = 历史实验残留（旧架构——综合链路 stage72+
      79+80 已自发——标注不再修改）
④ 否定词表→反例统计模式 —— ✅ stage81 完成（2026-08-21）
    否定 = 独立河道（C22-01）非负向修正（stage57 削弱 K 连累正向检索）
    ——"不"是 stage79 自发枢纽（freq 103+弥散）→ 否定句自动进 K["不"]
    ——无 NEG_WORDS、无 find 顺序查找
    "没有"块率 0.86 涌现（缺失否定河道）；"不是"率 0.29 不成块（设计——
      单字"不"河道承担——语料扩充后成块消除污染）
    hub 选择 = 位置最前（"不是什么"→"不"在"是"前——否定河道）
    验证：K[不][冰]→热（冰不是热的）/K[没有][鱼]→腿耳/K[不][鱼]→鸟
    ——"苹果不是什么？"→'不'河道（位置最前 ✓）
    已知局限："不是"的"是"轻微污染 K[是]（0.01 级）；苹果电脑多义；
      "吗"问标记未建模（语料 1 次——零河道边缘）
    stage57 的 NEG_WORDS = 历史实验残留（标注不再修改）
    corpus_negation.txt = 否定语料（62 条——数据非词表——我生成）
⑤ 句尾表→预测骤降（stage46）
⑥ 模板生成并入综合链路（stage72 v4——模板生成端）
已知局限（stage79）："苹果→水"杂质（wiki 苹果电脑公司义+水果字面——多义，
  stage77 方向）；"天气"率 0.18 不成块（PMI 可解）；词块仅一轮 2 字合并
  （计算机→计算+算机——3 字词拆散）
```
