# 结构衔接文档（LAYERS.md）

> R137 层级公理化 → 本文件 = L0-L3 的结构定义 + 现象的结构化表达式（补链）。
> 表达式格式：**现象 = 结构(S) × 机制(L1) × 公理(A) × 参数**——每段可追溯台账编号。
> 方法论（R138）：表达式 = 推导链的显式化——**每个现象都能写成组合，无需新公理**（递归原则实证）。

## L0 公理（8）— 详见 CONJECTURES.md 层级总览

A0 底层语言（C120）| A1 原语方程（C1-01）| A2 沉积-侵蚀（C5-01）| A3 同步域（C5-02）
A4 价值统一（C85-01）| A5 选择原语（C114-01）| A6 转导边界（C7-01）| A7 当下性（C109-01）

## L1 机制（6 组）— 详见 CONJECTURES.md

学习动力学（A2 族）| 价值系统（A4 族）| 同步传播（A3 族）| 方向涌现（A1 族）| 递归原则 | 候选生成

## L2 结构（7）— 详见 CONJECTURES.md

S1 决策环 | S2 博弈耦合 | S3 认知标记谱 | S4 社会湖 | S5 记忆地形 | S6 文化转导 | S7 意义语言

## 现象表达式

### 社会域（R138 补链）

| 现象 | 表达式 | 猜想 |
|---|---|---|
| 道德 | S1③ 评估 × S4 社会价值河道（训练扩展效用函数）× A4 × 双环训练（L1）——无独立模块 | C55-01 |
| 从众 | S1①(S2 他人行为 = 社会证据注入) × S1③ 排斥代价（威胁 A4）× 参数（排斥敏感度 R56）——双候选比较 | C59-01 |
| 地位 | S2 博弈互嵌 × S5 能力证明沉积（状态连续化）× L1 记忆缓存 × A4——信号沉积博弈 | C69-01 |
| 群体智慧/愚蠢 | Σ独立 S1（多样性）× S2 沟通 × S4 结构 \| 趋同 = 同步度→1 多样性→0 | C79-01 |
| 说服 | S2 输入侧操作 × L1 桥建立（沉积）× S7 挂载兼容 × 参数（双路径 ELM：中央=理性桥/外周=价值桥） | C92-01/C93-01 |
| 服从 | S1③ 评估 × 责任参数（"我的"权重下调——代价折扣）× S4 权威河道——正常决策 | C94-01 |
| 同理心 | L1 重构镜像（S7 理解的情绪版）× A4 情绪信号（经转导）→ 认知/情绪可分离（两河道） | C91-01 |
| 说谎 | S1⑤ 表达选择 × 忠实度参数（A5 选择）× S2 输出侧操控——与诚实同机制 | C73-01 |
| 宗教 | S1 评估 × 关闭验证环节（R49 验证循环终止）× S4 群体同步 × R53 死亡威胁（A4）——确定性结构桥 | C68-01 |
| 孤独 | S4 断水 × 多通道：S1① 需求-决策矛盾 + L1 信息匮乏（奖励不足）+ A4 价值断流 + R53 慢性警报 | C89-01 |
| 代沟 | S5 早期骨架（关键期定型）× R103 挂载兼容（新河道挂旧骨架）× A7 当下评估 | C87-01/C88-01 |
| 第一印象 | L1 记忆增益 × 首次共现全局标记（τ——C19-01 社交应用） | C21-02 |
| 笑 | A4 安全信号 × S2 信号广播（安全声明）× 参数（社会笑 = 信号无需内在情绪） | C65-01 |
| 信任 | S1③ 评估 × S5 对方行为河道 × A4 价值信号 × A7 当下性 → S3 可信标记（状态非参数） | C118-01 |
| 谣言 | S3 真假标记（一致性检验）× L1 读取-重组（S5 重建）× S1⑤ 转发选择——真实谣言无分别 | C117-01 |
| 博弈 | S2 = S1 互接（题目-参考答案变形）——不作用于个体内部 | C116-01 |

### 认知域（R139 补链）

| 现象 | 表达式 | 猜想 |
|---|---|---|
| 意识 | A3 大湖整合态 × 维持闭环 × S1 广播（g/v 过滤）——觉知 = 广播内容；睡眠 = 大湖解体 | C40-01 |
| 梦 | L1 重放 × 约束缺失（g 失效/大湖解体/现实校验缺）→ 无约束级联；梦 = 素材生成器（清醒建桥——R75 时间轴） | C42-01/C75-01 |
| 理解 | S7 重构 × R49 验证循环 × S2 对话互接 × 参数（重构保真度）——"我懂了" = 行为验证收敛非内心事件 | C43-01 |
| 主观时间 | L1 惊讶密度（R10 误差）× R52 焦点-信息耦合对 × A7 当下性——双模式（当下=速率/回顾=计数） | C45-01 |
| 直觉 | L1 强河道先行（部分激活）× S1③→⑤ 评估未完成提前输出 × 参数（领域河道密度）——说不出理由 = 验证循环未执行 | C72-01 |
| "为什么" | S1 评估 × 惊讶（R10）× 结构桥请求 × A4 安全锚点——儿童多问 = 惊讶密度高 + 关键期过水 | C74-01 |
| 感受 | A3 激活的体验侧 × 参数（激活私密性——难问题结构面）——激活即体验（色盲思想实验） | C48-01 |
| 他人意识 | A3 同构推理 × S5 内容差异（个体地形）× 参数（性格 = 参数配置）——内容个体化 | C49-01 |
| 认知层级 | S5 重构循环 × S3 必然标记（R45）× 黑天鹅（R10 强惊讶）——重组≠删除（特例桥挂载） | C50-01 |
| 自由意志 | A7 × A6 硬编码终止 × R59 自反闭合 × 参数（链条位置）——自由 = 链条中自我可及部分 | C51-01 |
| 创造力 | L1 桥事件（跨区域相关超阈）× S7 布局密度（Pasteur）× 约束松弛 × 参数（g/τ/锚定）——围棋 = 小湖合并认知版 | C47-01 |
| 记忆错误 | S5 重建 × L1 侵蚀不均 × 参数（锚定度 R30）——每次重建修改（结构完整性驱动近义词） | C58-01 |
| 婴儿失忆 | S5 重建失败（无语言锚）× L1 修剪 × 参数（神经新生 Akers 2014）——碎片 = 高标记事件 | C76-01 |
| 语言思维 | S7 符号湖（最大跨域枢纽）× S5 河道结构 × 参数（密度占比）——语言不塑造思维，结构塑造（语言 = 结构影子） | C78-01 |

### 价值域（待补链）

### 学习域（待补链）

### 病理域（待补链）

### 状态域（待补链）
