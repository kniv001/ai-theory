# 目标锚定（GOAL.md）

> 本文件是项目的"北极星"。每次讨论前先读。任何改动需记录原因。

## 北极星目标

构建一个与现有语言模型/多模态模型在**底层机制**上根本不同的新型 AI 模型理论框架。
"根本不同"的判定标准：核心计算原语、信息流、学习机制中至少一个层面是**全新构造**，
且新机制之间内在一致（不是现有机制的拼装）。

## 红线（不可违背）

- 不把 Transformer / 注意力机制作为核心计算单元（可作为批判对象与对比参照）
- 不把"加权求和 + 非线性激活"当作唯一计算原语（可替代、可推广）
- 框架自洽：每个核心机制必须给出"为什么必须这样"的论证，禁止循环论证
- 能力主张必须落实到机制层面（"可解释"要能指到具体结构），拒绝口号

## 设计约束（锚定问答结论，2026-08-16）

- **重构层**：全栈重构 — 计算原语、连接/信息流架构、学习算法、表示/认知结构四层全部重新设计
- **首要动机（2026-08-16 修正）**：持续推理、可注入输入、非函数化 —
  模型不是"输入→推理→输出"的闭函数；推理是一个**不间断的持续过程**，
  输入可在推理中途注入，输出可在任意时刻读出。标准形式：
  输入a → 推理 → 注入输入b → 推理 → 输出b → 推理 → 输出a。
- **次级约束**：可解释与可审计 — 推理过程在结构上可理解、可追责（支撑性质，服从首要动机）
- **出发点**：生物启发 — 从大脑的计算组织提取抽象原则（非照搬生物细节）
- **验证形式**：理论推理优先 — 先完成自洽的理论框架，暂不绑定形式化证明/仿真；框架有底后再定 M5 验证形式

**推论**（全栈重构的直接后果）：红线"至少一层全新"自动满足；但四层必须**内在一致**——
统一性成为全项目最大的风险点，任何一层设计都必须与其余三层可衔接。

**推论 2**（首要动机的直接后果）：任何一层都不能是"静态映射"——计算必须有**内在时间维**：
状态持续存在；输入是**注入到进行中计算的扰动**（非函数实参）；输出是**从演化状态的读出**（非函数返回值）；
输入与输出的对齐由过程动力学决定，不保证与输入顺序一致（示例中：输入a先到，但输出b先于输出a）。

## 里程碑（M1–M5）

- **M1** 核心计算原语：数学形式、必要性论证、性质清单
- **M2** 网络结构：原语组织成网络（拓扑 / 层 / 信息流 / 记忆结构）
- **M3** 学习机制：参数获得方式（与 M1/M2 自洽，含对反向传播的裁决）
- **M4** 能力论证：可实现能力的边界 + 与现有模型的结构性对比
- **M5** 验证：按锚定的验证形式执行

## 验收标准

- 任意核心机制存在完整论证链（前提 → 推导 → 结论），无循环
- 与现有模型存在**结构性**差异（非参数/规模/数据差异）
- 每个能力主张能指出承载它的具体机制
- 至少一份"为什么现有结构达不到"的对比论证
- 首要动机的标准形式（输入a-推理-输入b-推理-输出b-推理-输出a）必须是框架的**自然表达**，
  而非事后附加的交互层

## 非目标（现阶段明确不做）

- 工程实现与训练（除非验证形式要求玩具仿真）
- benchmark 竞赛式评测
- 追求对现有模型在已知任务上的增量改进
