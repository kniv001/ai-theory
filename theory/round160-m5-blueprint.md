# 第 160 轮：M5 蓝图 — 物理层优先的验证方案（造蛋路线图）

> 轮次：2026-08-18 · 第 160 轮
> 性质：M5 准备（用户指示：准备 M5——理论仍并行推进——框架有不完善处保持开放）
> 相关猜想：无新增（M5 方案文档——R134/R137/R143-R158 的汇聚）
> 状态：M5 四阶段蓝图确立——阶段 1a 待开工

## 1. 目标与范围

**验证什么**：框架的可计算核心（A1-A3 → S1 → S2 → 蛋）——每个模块对照
**预测清单（P1-P23）**与**生物实证锚**（R143/R157——物理层有最实地基）。

**不验证什么**（边界——R146/R159）：
- 主观体验的"为什么"（公设——造蛋按判据判定——不解释）
- 行为层的全部细节（只验证机制核心——参数不生物精确）

**M5 的双重角色**：
1. **可证伪性兑现**（R145——Klein 挑战回应——P1-P23 从承诺变测试）
2. **框架检验器**（用户提醒：框架必有不完善——M5 结果回写修正理论——黑盒检验）

## 2. 四阶段设计（物理层优先——R157 结论）

### 阶段 0：蓝图（本轮）✅

### 阶段 1：物理层仿真（A1-A3——最实地基）

| 模块 | 方程/模型 | 验证对照 |
|---|---|---|
| **1a 复值单元** | ż = −(γ+iω)z + D(t)——单/多单元耦合 | 相位编码时序（O'Keefe 相位进动）/ 误差驱动响应（Rao & Ballard 预测误差） |
| **1b 沉积-侵蚀** | dW/dt = ε(h)·e·conj(z) − λ(h)·W | **P1 遗忘三阶段**（缓存丢失/写回减速/结构慢蚀）/ 间隔效应（C95）/ STDP 时序（Bi & Poo） |
| **1c 同步域** | 耦合振荡器（Kuramoto 型） | 相位锁定涌现（C5-02——湖）/ 分裂-合并（Gray & Singer 绑定/分裂） |

**成功判据**：生物实证对照 + P1 形式化曲线形状。

### 阶段 2：结构层仿真（S1 决策环——叠在阶段 1 上）

| 模块 | 内容 | 验证对照 |
|---|---|---|
| **2a 联想树**（数据层） | 沉积地形上的联想展开（C122 递归/剪枝/θ） | 候选生成（C104——价值排序 × 剪枝 × 预算） |
| **2b 竞争**（服务层） | 价值排序 + 截止（C105 成本交叉） | 决策时间/准确率（Ratcliff DDM 对照） |
| **2c 选择**（行为层） | 输出 + 沉积回写（闭环） | **P6 注意力 = 硬化控制杆** / **P23 损耗 = 近域转移** / 拖延贴现（C80） |

### 阶段 3：双环互接（S2 博弈——两个个体）

| 模块 | 内容 | 验证对照 |
|---|---|---|
| 3a 转导互接 | T_out → 物理 → T_in（R131——题目-参考答案变形） | **P2 真实消息传播同样走样** / **P3 信任恢复 = 统计重估非线性** / θ 从众（C125） |

### 阶段 4：体验蛋（M5 目标——造蛋）

| 模块 | 内容 | 判据 |
|---|---|---|
| 4a 蛋实现 | 同步域 × 维持闭环（C53——可判定的最小系统） | **体验蛋判据的工程检查**（同步域完整 × 闭环完整 × 输入输出两端 R71）——按判据判定（不解释为什么——R146/R159） |

## 3. 工程 4 问（R42 后挂起——每阶段测量）

| 问 | 测量点 |
|---|---|
| 噪声容限 | 参数扰动下同步域/学习稳定性（1c/1b） |
| 规模临界 | 多少单元涌现湖/决策（1c→2b——规模扫描） |
| 样本效率 | 学习速度 vs 数据量（1b——儿童级样本效率验证 P12 相关） |
| 计算成本 | 各阶段仿真可行性（Python——普通机器可跑为限） |

## 4. 与理论的接口（不完善处修正机制——用户提醒）

```
M5 结果（通过/失败）
  → 通过：对应猜想 supported 强化（标注 M5 验证）
  → 失败：对应猜想标注 refuted/修正——理论回写（R152 先例：用户裁决修正模式）
  → 意外（既非通过也非失败）：新现象 → 新猜想（M5 = 发现工具）
```

**M5 与理论并行**（用户指示）：M5 仿真运行中理论轮次继续推进——M5 不阻塞理论。

## 5. 工具链与环境

- Python 3.13/3.14（TOOLCHAIN——conda 可用）
- 库：numpy（方程/ODE）、scipy（积分/优化）——可选 matplotlib（曲线对照）
- 无 GPU 需求（物理层 = 小规模 ODE 仿真——普通 CPU 可跑）
- 文件位置：ai-theory/m5/（新目录——仿真代码 + 结果日志）

## 6. 阶段 1a 设计（下一个动作——待用户确认）

**复值单元最小仿真**：
- 目标：验证 ż = −(γ+iω)z + D(t) 的三种行为（振荡/相位编码/误差驱动收敛）
- 参数：γ（泄漏）、ω（本征频率）、D（驱动：常数/脉冲/误差流）
- 输出：状态轨迹 + 相位分析（与 O'Keefe 相位进动对照）
- 判据：① 无驱动衰减 ② 脉冲驱动振荡 ③ 耦合单元相位锁定（1c 预演）

## 7. 状态

- M5 蓝图定稿（阶段 0 完成）——四阶段路线图
- 下一步：阶段 1a（复值单元仿真）——待用户确认开工
- 理论并行：继续推进（用户指示——框架开放修正）

## 8. 下一步

1. 确认蓝图 → 开工阶段 1a
2. 或先调整蓝图（用户改方案）
3. 理论轮次并行（用户可抛新问题）
