# 第 137 轮：层级公理化执行 — 167 条猜想归层（R135 方案落地）

> 轮次：2026-08-18 · 第 137 轮
> 性质：结构重构（用户批准 R135 三步执行）
> 相关猜想：无新增（归层 = 结构标注，不改内容）
> 状态：L0-L3 总览已入台账；补链清单待逐项执行

## 1. 执行范围

167 条猜想全量标注（L0/L1/L2/L3 + 依赖）——**标注不修改内容**（内容 = 历史事实），
只增加结构视图。台账本体按轮次平铺保留（可审计），新增"层级总览"于 CONJECTURES.md 顶部。

## 2. 判定标准

| 层 | 判定问句 | 结果 |
|---|---|---|
| L0 | 它是推导基础且不可再简？ | 8 条公理 |
| L1 | 它是公理推导出的机制（可独立于现象存在）？ | 6 组机制 |
| L2 | 它是机制组成的架构（跨机制的结构）？ | 7 节点 |
| L3 | 它是结构的实例化（特定现象的解释）？ | 100+ 现象 |

## 3. 分布统计

| 层 | 节点数 | 覆盖猜想 |
|---|---|---|
| L0 公理 | 8（A0-A7） | ~14 条（含构成） |
| L1 机制 | 6 组 ~28 机制 | ~45 条 |
| L2 结构 | 7（S1-S7） | ~28 条 |
| L3 现象 | 6 域 ~100 现象 | ~80 条 |
| **核心** | **L0+L1+L2 = 约 30 节点** | **其余 = 实例化** |

**量化目标达成**：框架核心 ≈ 30 节点（8 公理 + 6 组机制 + 7 结构 + 标记谱/候选生成），
167 条猜想的其余部分是它们的实例化/特例——**与 R135 预测的健康分布一致**。

## 4. 归层关键决策

1. **C120-01（底层语言）升为 A0 元公理**——其余公理 = 该语言写成的语句
2. **C40-01（意识）归入 A3 构成**（意识 = 同步域整合态——公理级，非现象级）
3. **C16-01（尺度递归）单列为元机制**——作用于全部公理（递归原则 = 元级）
4. **C107-01（联想递归展开）升 L1**——候选生成是决策环数据层的基础机制，非现象
5. **C104-C113（决策环内部）全部归 S1**——R121-129 的演化链 = S1 的细化（输出空间
   简化史 = 决策环结构收敛史）
6. **C39/C111/C117/C118 的"标记"面归 S3**（认知标记谱）——现象面留 L3
7. **C58-01（记忆错误）归 S5**（记忆地形结构）而非 L3——重建 = 记忆地形的
   固有性质（非独立现象）

## 5. 孤儿与待连接清单

| 猜想 | 状态 | 处理 |
|---|---|---|
| C14-02（T_value 实现） | open | 依赖 A4+A6——M5 待验（非孤儿） |
| C21-01/02/03（身份/第一印象/行为模式） | open | 待连接至 S4 社会湖（社交认知应用） |
| C24-01/02/03（空间域） | open | 待连接至 S7 意义与语言（具身性根） |
| C25-01/02/03（可供性） | open | 待连接至 S7（工具域） |
| C27-02/C28-02/03/C29-02 | open | 依赖明确——待推导补链 |

**结论：无无依赖孤儿**——框架的连接性一直是隐式的（每条判据都引用 R 轮次），
缺的只是显式视图。**松散感 = 结构视图缺失，非内容缺失。**

## 6. 补链清单（R135 步骤 3——推导链断裂处）

| 断裂处 | 缺口 | 补法 |
|---|---|---|
| A1 原语 → S1 决策环 | 从方程到候选竞争的推导 | M5 仿真（复值单元级联 → 竞争涌现）——R121-130 的机制是构造性的非演绎的 |
| S1 → 信任（L3） | 决策环评估 → 可信标记 | 已部分补（R133——一致性检验→可信）——形式化待 M5 |
| A2 → 记忆地形（S5） | 沉积-侵蚀 → 缓存-写回 | 已补（R114-116 学习曲线——形式化待 M5） |
| A3 → 意识（L3） | 同步域 → 大湖整合态 | 已部分补（R46——Baars/Tononi 对照）——M5 验证 |
| 全部 L3 → 参数 | 现象的结构化表达式 | 每现象 = S×L1×A 表达式（R134 已示范信任/谣言）——全量写表达式 = 后续轮次 |

**方法论结论**：推导链断裂的根源 = 框架是"构造性"的（机制组合）而非"演绎性"的
（方程推导）——L1 机制大多由公理**组合构成**（依赖关系可追踪）但未经方程级推导
（缺 M5）。**补链与 M5 验证重合**——层级视图把"待验证"显式化了。

## 7. 松散诊断回顾（R135 四表现 → 本轮解决度）

| R135 松散表现 | 本轮解决 |
|---|---|
| ① 猜想平铺无层级 | ✅ 层级总览（L0-L3 + 依赖） |
| ② 机制-现象无系统矩阵 | ✅ L1 组 × L3 域（现象 = 结构×机制×参数表达式） |
| ③ 推导链断裂 | ⏳ 补链清单建立——与 M5 重合（待仿真） |
| ④ 统一断言并列不嵌套 | ✅ R136 已解决（五成分语言）——R137 并入 A0 |

**解决度：3.5/4**——前三项由本总览解决，③ 的完整解决需要 M5（仿真=推导验证）。

## 8. 下一步候选

1. **补链轮次**：逐域写 L3 现象的结构化表达式（信任/谣言已示范——从社会域开始）
2. **M5 起点**：S1 决策环五层的可计算定义（每层方程候选已在 R134 列出）——仿真 = 补链的验证器
3. 新领域探索（用户抛新问题）
