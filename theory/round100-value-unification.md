# 第 100 轮：价值统一 — 生存警报与趋利 = 同一价值信号

> 轮次：2026-08-17 · 第 100 轮
> 提出者：用户（生存警报与趋利是同个事物——生存在趋利里优先级极大）
> 相关猜想：C85-01（新增；简化 R99）
> 状态：价值统一确立

## 1. 统一（用户修正）

| R99（两信号竞争） | 统一后（一个信号 + 权重） |
|---|---|
| 生存警报 vs 趋利 | 同一价值信号（RPE/R8——正负两端：快乐=正、威胁=负） |
| 两机制竞争决定修改权 | 一个价值信号的不同权重段——生存 = 价值函数中权重最大的项（R36 稳态） |

"生存警报" = 价值信号高权重段（负端大值）；"趋利" = 一般段——同一轴。

## 2. 框架统一语言再添一笔

误差唯一词汇 + **价值唯一信号**（生存 = 最大权重项）——
记忆修改权（R99）= 价值信号权重函数（生存相关 = 高权重保护；低权重 = 可修改）——
一切动机（趋利/避害/生存/快乐/恐惧）= 同一价值信号的参数。

## 3. 猜想

| ID | 猜想 | 状态 | 判据 |
|---|---|---|---|
| C85-01 | 价值统一：生存警报与趋利 = 同一价值信号（RPE/R8——正负两端）；生存 = 价值函数中权重最大的项（R36——非另一类价值）；R99"竞争" = 同一信号高权重段 vs 低权重段；记忆修改权 = 价值信号权重函数；框架统一语言：误差唯一词汇 + 价值唯一信号 | supported | 用户统一 + R8/R36/R19 一致 |
