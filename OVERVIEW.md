# 大湖群框架总论（OVERVIEW v1）

> 版本 v1：R1–R19 全景综述（2026-08-16）
> 本文件是理论的**单一事实源入口**——先读这里；细节链接到 theory/round*.md，锚定见 GOAL.md，猜想见 CONJECTURES.md

## 0. 一句话

一个与 LLM/多模态模型根本不同的 AI 模型理论：**误差是唯一词汇，关系是唯一结构，通道分广播与河道，机制在所有尺度递归。**

## 1. 北极星与锚定（GOAL.md 摘要）

- **目标**：全栈重构（计算原语 / 连接架构 / 学习算法 / 表示认知结构），与现有模型**根本不同**
- **首要动机**：可注入的持续过程，非函数化——输入a-推理-输入b-推理-输出b-推理-输出a，推理不间断
- **次级约束**：可解释与可审计（机制层面，拒绝口号）
- **出发点**：生物启发（提取抽象原则，非照搬细节）
- **验证形式**：理论优先（框架有底后再定 M5 形式）

## 2. 统一性论点（升级链）

| 版本 | 论点 | 轮次 |
|---|---|---|
| v1 | 动力学是统一语言（计算=轨迹 / 结构=耦合 / 学习=塑造动力学 / 表示=流形） | R1 |
| v2 | 误差是唯一词汇，通道是两种语法（广播 / 河道） | R10 |
| v3 | 关系是唯一结构（尺度递归：字→词→句→篇章同机制，无独立语法模块） | R16 |

> 全框架只有一个信号（**误差**）、两类通道（**广播与河道**）、一个结构（**预测关系网络，分形递归**）。

## 3. 架构总览：边界-湖-河-地形

**边界**（R7）：硬编码转导——感官特异机器（视觉/听觉/伤害）→ 共同货币（复值网络语言）。
T_in（世界→网络）、T_out（网络→行为，感觉-运动闭环）、T_value（伤害/奖励→v）。
原则：**边界函数化，内部过程化**（"生物=函数"只在边界成立）。
文化转导层（文字字形 = 视觉切割）见 R17。

**湖**（R5/R6/R16）：相位锁定群体（同步域）。域级湖（模态划分）· 概念级湖（语义划分）；
湖间关系类型学：**关联**（跨域，强河道）vs **嵌套**（域内，子湖生长）；
小湖合并 = 同步（Huygens/Kuramoto）；功能分块涌现（硬度场分化 → 长时/短时记忆区）；
词类子湖 = 分布聚类涌现。

**河**（R2/R4/R10）：因果影响链（模式再生，物质不流，动作电位式接力）。
预测河（下行，**注意闸门**控制，心象需注意）· 误差河（上行，自动）。
河流时间轴四重结构：传导延迟 / 局部变换 / 累积变形 / 生命周期。
双通道架构：**广播通道**（调质/价值，全局快无方向=弥散网化石）管状态模式，
**河道通道**（预测/误差）管内容结构。

**地形**（R5/R6/R11）：参数场 {γ, ω, W} + 硬度场 h + 标记 τ。
沉积-侵蚀方程；分流饱和（结构涌现的刚性）；记忆 = 地形本身。

## 4. 原语（M1 交付，R3）

$$\dot{z} = -(\gamma + i\omega)z + D(t), \qquad z \in \mathbb{C}$$

- γ 泄漏（τ=1/γ 时间常数）/ ω 本征频率（震荡=时序维度）/ D 驱动
- **载体**：带时序结构的连续场（幅度=强度、相位=时序、频率=时间尺度）——C1-03
- **驱动**：D = 误差 = 现实 − 预测；预测 = 内部加权流（分层裁决）——C1-01
- 权重 W∈ℂ：|W|=连接强度、arg W=传导延迟（延迟内建、被学习）
- 事件式（脉冲）= 读出层扩展，非核心载体

## 5. 学习机制

| 机制 | 方程/内容 | 轮次 |
|---|---|---|
| 沉积-侵蚀 | dW/dt = ε(h)·e·conj(z) − λ(h)·W（强度与延迟同时学） | R5 |
| 元可塑性 | dh/dt = 稳定性 − 新奇性 + 稳态回拉（向 h₀）+ 标记加速 | R6/R11 |
| 双环反馈 | 外环自然选择（群体级沉积-侵蚀，自举价值系统）+ 内环价值系统（RPE） | R8 |
| 调制 | 注意闸门 g（误差驱动）/ 调质广播 m（γ、ε 调制）/ 价值 v（ε 门控 + τ 标记） | R10/R11 |
| 记忆增益 | M = Σ 单次增益，g ∝ 到生存核心湖群的河道接近度（统一双相固化） | R19 |
| 睡眠 | 下行缩放（清噪恢复软区）+ 重放固化（标记河道硬化） | R18 |
| 分流饱和 | 可塑性下降 = 结构涌现（既有河道吸走流量）——关键期机制 | R18 |

## 6. 记忆与意义

- **记忆 = 地形本身**（C6-03）：硬区=结构记忆（W 沉淀）、软区=动态记忆；固化=软→硬重复冲刷
- **意义 = 预测关系集**（C13-01）：存储的是关系不是实体；审计一个字 = 读它的河道子图
- **组合性 = 时序吸引子复合**（C13-02）；语法 = 更高尺度的关系（C16-01，尺度递归）
- **具身性 = 符号湖↔感觉湖河道密度**（C13-03，人类高、LLM≈0、连续谱）
- **学习不需要奖励**（C14-01）：误差驱动=基础学习（潜伏学习）；奖励=重要性标记

## 7. 时间尺度梯子（R4/R18）

| 尺度 | 作用 | 修复 |
|---|---|---|
| 进化（百万年） | 外环筛选整套先验（h₀/T/V），群体级沉积-侵蚀 | 群体级演化 |
| 发育（月-年） | 关键期大塑形 = 结构饱和（分流）+ 高 ε | 睡眠 |
| 经验（天-周） | 持续塑形（痕迹衰减） | 睡眠 |
| 活动（毫秒） | 快调制（闸门/调质） | — |

饱和-修复循环**三尺度同构**（R18：进化/一生/一天）。

## 8. 与 LLM 的结构性差异（"根本不同"清单）

1. **函数 vs 过程**：非函数化（可注入持续过程）——首要动机
2. **训练/推理边界消失**：结构是训练产物（C2-02），只有时间尺度之分
3. **价值系统自举 vs 手工损失函数**：双环（C8-01/05）
4. **记忆=内建地形 vs 全局权重+外部 KV cache**（C6-03）
5. **语义可扎根 vs 无根**：具身性=河道密度（C13-03）
6. **推理期拓扑演化 vs 固定连接图**：河流产生/断流/改道（C2-01）
7. **时间一等公民**：相位/延迟内建（W∈ℂ）vs 位置编码
8. **方向性 = 学习前提**：无方向系统只能习惯化（C9-01）

## 9. M4 案例：学会"山"（R12 摘要）

五阶段：先天地形 → 共现刻河（惊讶+注意+标记）→ 重复巩固（四湖相位锁定合并）→ 泛化（吸引子流形）→ 输出（感觉-运动闭环）→ 固化（睡眠+标记）。

**全机制首检通过**：R1–R19 机制协同无矛盾；"学习一个词"= 一次完整的湖群自组织。
缺口：湖群嵌套（部分解决：跨域=关联，R17）、文化转导层、关键期包络数学。

## 10. 猜想台账（67 条）

- 已裁决 supported：C0-01（连续时间）/ C1-01（分层驱动）/ C1-03（复值载体）/ C2-03/05/06（环境裁决）/ C4-03（塑形两层）/ C5-01/03（沉积-侵蚀）/ C6-…（硬度裁决）/ C7-01（边界原则）/ C8-01/02/03（双环）/ C9-01/02（进化）/ C10-02/03（调制）/ C13-01（意义）/ C14-01（无奖励学习）/ C15-01/03（语法）/ C16-01（尺度递归）/ C17-01（视觉切割湖）/ C18-01/02（饱和/睡眠）/ C19-01（记忆增益）
- 其余 open（待 M5 仿真或后续推导验证）
- 完整台账：CONJECTURES.md

## 11. 里程碑状态

| 里程碑 | 状态 |
|---|---|
| M1 原语 | ✅ v1 完成（R3） |
| M2/M3 联合推导（分支） | ✅ 试行通过（R5–R12，机制首检无矛盾） |
| M4 能力论证 | 🔄 语义学原理确立；"山"案例预演（R12/R13） |
| M5 验证 | 待定（理论优先） |

## 12. 开放问题

1. 湖群嵌套：符号湖与语言湖群的关联细节（部分解决）
2. 文化转导层：文字=第二转导层的完整形式化
3. 关键期包络数学（ε 的发育时间函数）
4. 跨尺度河道：词如何继承字的预测关系
5. 组合性层级递归（从句/篇章的模板嵌套）
6. 具身性阈值（多少河道密度算"扎根"）
7. 身体模型参数（内感受：方案 A）
8. 语义桥定量（v_t 与河道距离的函数）
9. 结构塑性出生竞争（多个候选连接同时超阈）
10. 重放触发机制（标记优先？）

## 13. 文档地图

- [GOAL.md](GOAL.md) 锚定与红线 | [CONJECTURES.md](CONJECTURES.md) 猜想台账 | [SESSION.md](SESSION.md) 轮次日志 | [TOOLCHAIN.md](TOOLCHAIN.md) 工具
- [theory/](theory/) 各轮细节（19 篇：R1 原语候选 → R19 记忆深度）
